@model Holix.Models.Project
@{
    ViewData["Title"] = Model.Title;
    var relatedProjects = ViewBag.RelatedProjects as List<Holix.Models.Project> ?? new List<Holix.Models.Project>();
    string FormatViews(int views)
    {
        if (views >= 1000)
            return (views / 1000.0).ToString("0.#") + "k";
        return views.ToString();
    }
}

<canvas id="portfolioBgCanvas"></canvas>
<div id="scroll-progress"></div>

<div class="blog-bg-circles">
    <div class="blog-bg-circle blog-bg-circle-1"></div>
    <div class="blog-bg-circle blog-bg-circle-2"></div>
</div>

<section class="blog-page-section">
    <div class="container">
        <div class="row justify-content-center position-relative">
            <div class="col-lg-8" data-aos="fade-up">
                <div class="mb-4 post-meta">
                    <span class="text-uppercase small text-primary fw-semibold tracking-wider">
                        <i class="fa-regular fa-calendar me-1 icon-calendar"></i> @Model.CompletedAt.ToString("MMMM yyyy")
                    </span>
                    <span class="text-secondary small mx-2">&middot;</span>
                    <span class="text-secondary small">
                        <i class="fa-regular fa-building me-1 icon-building"></i> @Model.Client
                    </span>
                    <span class="text-secondary small mx-2">&middot;</span>
                    <span class="text-secondary small">
                        <svg class="icon-views" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: text-top;"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        <span class="ms-1">@FormatViews(Model.Views)</span>
                    </span>
                </div>

                <h1 class="fw-bold display-5 mb-4 lh-1 post-title">@Model.Title</h1>

                @if (!string.IsNullOrEmpty(Model.Technologies))
                {
                    <div class="mb-4 d-flex flex-wrap gap-2">
                        @foreach (var tech in Model.Technologies.Split(','))
                        {
                            <span class="portfolio-tech-tag portfolio-tech-tag-lg">@tech.Trim()</span>
                        }
                    </div>
                }

                @if (!string.IsNullOrEmpty(Model.Excerpt))
                {
                    <p class="fs-5 text-secondary mb-5 lead post-excerpt">@Model.Excerpt</p>
                }

                @if (!string.IsNullOrEmpty(Model.ImageUrl))
                {
                    <div class="mb-5 rounded-4 overflow-hidden shadow-sm parallax-wrap">
                        <div class="parallax-image" style="background-image: url('@Model.ImageUrl');"></div>
                    </div>
                }
                else
                {
                    <div class="mb-5 rounded-4 overflow-hidden" style="height: 200px; background: linear-gradient(135deg, var(--primary-color), #059669);"></div>
                }

                <div class="blog-content fs-5">
                    @Html.Raw(Model.Description)
                </div>

                <div id="content-end-marker"></div>

                <hr class="my-5">

                <div class="d-flex justify-content-center gap-2">
                    <a asp-controller="Portfolio" asp-action="Index" class="btn btn-outline-primary rounded-pill px-4 btn-premium d-inline-flex align-items-center justify-content-center">
                        <i class="fa-solid fa-arrow-left me-2"></i>Back to Portfolio
                    </a>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary rounded-pill px-3 btn-premium d-inline-flex align-items-center justify-content-center">
                        <i class="fa-solid fa-home me-1"></i>Home
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

@if (relatedProjects.Any())
{
    <section class="py-5 related-posts-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="section-title-line"></div>
                    <h2 class="fw-bold fs-2 mb-4">Related Projects</h2>
                    <div class="row g-4">
                        @for (int i = 0; i < relatedProjects.Count; i++)
                        {
                            var rp = relatedProjects[i];
                            <div class="col-md-4" data-aos="fade-up" data-aos-delay="@(i * 100)">
                                <div class="portfolio-card h-100">
                                    @if (!string.IsNullOrEmpty(rp.ImageUrl))
                                    {
                                        <div class="portfolio-card-image">
                                            <img src="@rp.ImageUrl" alt="@rp.Title">
                                            <div class="portfolio-card-overlay"></div>
                                        </div>
                                    }
                                    <div class="portfolio-card-body">
                                        <span class="portfolio-date text-secondary small">@rp.CompletedAt.ToString("MMM yyyy")</span>
                                        <h5 class="fw-bold mt-2 mb-1">
                                            <a asp-controller="Portfolio" asp-action="Details" asp-route-slug="@rp.Slug" class="text-dark text-decoration-none">@rp.Title</a>
                                        </h5>
                                        <span class="text-secondary small">@rp.Client</span>
                                        @if (!string.IsNullOrEmpty(rp.Technologies))
                                        {
                                            <div class="mt-2 d-flex flex-wrap gap-1">
                                                @foreach (var tech in rp.Technologies.Split(','))
                                                {
                                                    <span class="portfolio-tech-tag">@tech.Trim()</span>
                                                }
                                            </div>
                                        }
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </section>
}

<div id="back-to-top" class="back-to-top-btn">
    <i class="fa-solid fa-arrow-up"></i>
</div>

<div id="reading-toast" class="reading-toast">
    <i class="fa-regular fa-circle-check me-2"></i> ¡Proyecto increíble! 🚀
</div>

@section Scripts {
    <script>
        (function() {
            var curtain = document.createElement('div');
            curtain.id = 'pageCurtain';
            document.body.prepend(curtain);
            requestAnimationFrame(function() {
                curtain.style.transform = 'translateY(0)';
            });
            setTimeout(function() {
                curtain.style.transition = 'transform 0.3s ease-out';
                curtain.style.transform = 'translateY(-100%)';
            }, 300);
            setTimeout(function() {
                curtain.remove();
            }, 600);
        })();

        (function() {
            var title = document.querySelector('.post-title');
            if (title) {
                var text = title.textContent;
                title.textContent = '';
                for (var i = 0; i < text.length; i++) {
                    var ch = text[i];
                    if (ch === ' ') {
                        var space = document.createElement('span');
                        space.className = 'letter-span';
                        space.innerHTML = '&nbsp;';
                        title.appendChild(space);
                    } else {
                        var span = document.createElement('span');
                        span.className = 'letter-span';
                        span.textContent = ch;
                        title.appendChild(span);
                    }
                }
                var letters = title.querySelectorAll('.letter-span');
                letters.forEach(function(span, idx) {
                    setTimeout(function() { span.classList.add('visible'); }, 600 + idx * 40);
                });
            }
        })();

        (function() {
            function wrapWords(root) {
                var walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, null, false);
                var nodes = [];
                while (walker.nextNode()) nodes.push(walker.currentNode);
                nodes.forEach(function(node) {
                    var text = node.textContent;
                    var parts = text.split(/(\s+)/);
                    if (parts.length < 2) return;
                    var frag = document.createDocumentFragment();
                    for (var i = 0; i < parts.length; i++) {
                        if (parts[i].trim() === '') {
                            frag.appendChild(document.createTextNode(parts[i]));
                        } else {
                            var span = document.createElement('span');
                            span.className = 'hover-word';
                            span.textContent = parts[i];
                            frag.appendChild(span);
                        }
                    }
                    node.parentNode.replaceChild(frag, node);
                });
            }
            var targets = document.querySelectorAll('h1, h2, h3, h4, p, span, li, a, label, small, strong, em, blockquote, figcaption');
            targets.forEach(function(el) {
                if (!el || el.closest('.hover-word') || el.classList.contains('post-title') || el.querySelector('.hover-word')) return;
                wrapWords(el);
            });
        })();

        (function() {
            var bar = document.getElementById('scroll-progress');
            if (!bar) return;
            function update() {
                var scrollTop = window.scrollY;
                var docHeight = document.documentElement.scrollHeight - window.innerHeight;
                var pct = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
                bar.style.width = pct + '%';
            }
            window.addEventListener('scroll', update);
            update();
        })();

        (function() {
            var el = document.querySelector('.parallax-image');
            if (!el) return;
            function update() {
                var scrollY = window.scrollY;
                el.style.backgroundPositionY = (50 - scrollY * 0.04) + '%';
            }
            window.addEventListener('scroll', update);
            update();
        })();

        (function() {
            var backBtn = document.getElementById('back-to-top');
            if (!backBtn) return;
            function toggle() {
                if (window.scrollY > 300) {
                    backBtn.classList.add('visible');
                } else {
                    backBtn.classList.remove('visible');
                }
            }
            window.addEventListener('scroll', toggle);
            toggle();
            backBtn.addEventListener('click', function() {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            });
        })();

        (function() {
            var marker = document.getElementById('content-end-marker');
            if (!marker) return;
            var toast = document.getElementById('reading-toast');
            if (!toast) return;
            var obs = new IntersectionObserver(function(entries) {
                entries.forEach(function(entry) {
                    if (entry.isIntersecting) {
                        toast.classList.add('visible');
                        setTimeout(function() { toast.classList.remove('visible'); }, 3000);
                        obs.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.5 });
            obs.observe(marker);
        })();

        (function() {
            var canvas = document.getElementById('portfolioBgCanvas');
            if (!canvas) return;
            if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) { canvas.style.display = 'none'; return; }
            var ctx = canvas.getContext('2d');
            var w, h, lines = [];
            function resize() { w = canvas.width = window.innerWidth; h = canvas.height = window.innerHeight; }
            window.addEventListener('resize', resize);
            resize();
            function createLine() {
                return {
                    x: Math.random() * w,
                    y: Math.random() * h,
                    len: 80 + Math.random() * 120,
                    speed: 0.3 + Math.random() * 0.5,
                    opacity: 0.03 + Math.random() * 0.04,
                    direction: Math.random() > 0.5 ? 1 : -1
                };
            }
            for (var i = 0; i < 12; i++) lines.push(createLine());
            function update() {
                for (var i = 0; i < lines.length; i++) {
                    var l = lines[i];
                    l.x += l.speed * l.direction;
                    if (l.x > w + l.len) { l.x = -l.len; l.y = Math.random() * h; }
                    if (l.x < -l.len) { l.x = w + l.len; l.y = Math.random() * h; }
                }
            }
            function draw() {
                ctx.clearRect(0, 0, w, h);
                for (var i = 0; i < lines.length; i++) {
                    var l = lines[i];
                    ctx.strokeStyle = 'rgba(34, 197, 94, ' + l.opacity + ')';
                    ctx.lineWidth = 1;
                    ctx.beginPath();
                    ctx.moveTo(l.x, l.y);
                    ctx.lineTo(l.x + l.len * l.direction, l.y);
                    ctx.stroke();
                }
            }
            function loop() { update(); draw(); requestAnimationFrame(loop); }
            loop();
        })();

        (function() {
            var headings = document.querySelectorAll('.blog-content h2, .blog-content h3');
            if (!headings.length) return;
            headings.forEach(function(h) {
                if (!h.id) h.id = 'toc-heading-' + Math.random().toString(36).slice(2, 8);
                var wrap = document.createElement('div');
                wrap.className = 'heading-underline-wrap';
                h.parentNode.insertBefore(wrap, h);
                wrap.appendChild(h);
                var line = document.createElement('div');
                line.className = 'heading-underline';
                wrap.appendChild(line);
                var obs = new IntersectionObserver(function(entries) {
                    entries.forEach(function(entry) {
                        if (entry.isIntersecting) {
                            line.classList.add('heading-underline-visible');
                            obs.unobserve(entry.target);
                        }
                    });
                }, { threshold: 0.3 });
                obs.observe(wrap);
            });
        })();

        (function() {
            var blockquotes = document.querySelectorAll('.blog-content blockquote');
            if (!blockquotes.length) return;
            blockquotes.forEach(function(bq) {
                var border = document.createElement('div');
                border.className = 'bq-border';
                bq.style.position = 'relative';
                bq.insertBefore(border, bq.firstChild);
                var obs = new IntersectionObserver(function(entries) {
                    entries.forEach(function(entry) {
                        if (entry.isIntersecting) {
                            border.classList.add('bq-border-visible');
                            obs.unobserve(entry.target);
                        }
                    });
                }, { threshold: 0.3 });
                obs.observe(bq);
            });
        })();

        /* ── Icon Click Animations ── */
        (function() {
            function setupIcon(selector, activeClass) {
                var el = document.querySelector(selector);
                if (!el) return;
                el.addEventListener('click', function() {
                    this.classList.remove(activeClass);
                    void this.offsetWidth;
                    this.classList.add(activeClass);
                });
                el.addEventListener('animationend', function() { this.classList.remove(activeClass); });
            }
            setupIcon('.icon-calendar', 'icon-rotate-active');
            setupIcon('.icon-building', 'icon-bounce-active');
            setupIcon('.icon-views', 'icon-pulse-active');
        })();
    </script>
}
